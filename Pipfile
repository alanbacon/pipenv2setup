[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[scripts]
	sandbox = "pipenv run python sandbox.py"

[dev-packages]

[packages]
tomlkit = ">=0.0.1"

[requires]
python_version = "3.7"
